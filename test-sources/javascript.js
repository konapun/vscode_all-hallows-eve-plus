import test from 'test'

class TestClass extends Thing {
    constructor () {
        let str = 'string'
        let number = 90
        let obj = {
            key: number
        }
    }
}

function test (games) {
    const game = games.get(args.id)

    console.log('testing')
}

// line comment
/**
 * 
 * Block comment
 * 
 */
const fn = (param1, param2, param3) => {
    param1.map(val => val * 12)
}